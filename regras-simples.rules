rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regra temporária mais permissiva para testar o sistema econômico
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}